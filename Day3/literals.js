//WHAT ARE TEMPLATE LITERALS?
//Template literals are modern strings in JavaScript that make text + variables easy.
//Why template literals are better than normal strings?
// Easy variable insertion, multiline support, clean code.
//What is Template Literal?
//Template literals use backticks and allow variables inside strings.

let text = `Hello
Welcome
Anu`;
console.log(text)

const backtick=`Tom`
const single='Jerry'
const result1=`${backtick} and ${single}`
console.log(result1)

let name = "Safreen";
let mark = 90;
let result = `${name} scored ${mark} marks`;
console.log(result);
